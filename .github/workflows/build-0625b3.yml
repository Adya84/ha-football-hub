name: Build manual Football Hub changed-files ZIP

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/build-0625b3.yml

permissions:
  contents: write

jobs:
  build-zip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger check $(date -u)"
